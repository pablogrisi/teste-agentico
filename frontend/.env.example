# Base URL da API do backend LicIA Analisadora.
# NÃO é consumida nesta fundação (TSD-002): a camada de dados usa fixtures.
# Quando os contratos REST por RF existirem, o módulo de composição
# (src/lib/data/index.ts) passa a instanciar um gateway HTTP quando esta variável
# estiver definida. Deixe vazia para usar as fixtures.
NEXT_PUBLIC_API_BASE_URL=
