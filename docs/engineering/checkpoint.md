# Engineering Checkpoint - LicIA Analisadora

---

title: Engineering Checkpoint - LicIA Analisadora
type: checkpoint
status: ativo
created: 31/08/2026 // Pablo Grisi
updated: 02/09/2026 // Pablo Grisi (TSD-023 — entrada por texto extraído; aguardando merge + smoke real)
---

## 1. Estado atual

Fundação percorrida (`.ai-dev/bootstrap.md`): PRD e SDD aprovados, `questoes-abertas.md`, glossário, `quality-gates.md` e `context-map.md` preenchidos, decision record 001 (NestJS) registrado.

Projeto organizado em **duas frentes paralelas num monorepo**: `backend/` (NestJS, Pablo) e `frontend/` (Next.js, Vinicius), método e docs compartilhados na raiz. Cada frente roda seu próprio ciclo de seis papéis.

> **Marco (02/09/2026): o MVP frontend está completo.** As 12 linhas da tabela de sequenciamento do frontend (`roadmap.md`) estão `implementada — Crítico ✅`. `/` (listagem + "Nova análise") e `/analise/[id]` (visor de PDF + navegação por página, revisão com status sugerido pela IA, "Alterar parecer", "verificado" interativo, justificativa visível, referência de página clicável + editor, "Concluir análise" com trava, "Baixar relatório") cobrem todos os RFs de frontend. Pendências do MVP: a integração da IA real (backend, A-02 / TSD-022 + **TSD-023** — o adapter passou a mandar ao modelo o **texto do PDF por página**, com fallback automático para a Files API em PDF escaneado) — já implementada como `AnaliseIaOpenAiAdapter` na branch `backend/ia-openai`, aguardando merge + smoke ponta-a-ponta com a `OPENAI_API_KEY` real — e os follow-ups de acabamento (§5).

- **`main`**: TSD-001 + RF-006 + RF-001/004/018 + RF-002 + RF-005/RF-007 + RF-009 + RF-008/011/017 + **RF-014/TSD-009** + **RF-012/RF-013/RF-015/TSD-010** + **RF-016/TSD-011** integradas (backend — **features do MVP completas**). A **integração da IA real (A-02 / TSD-022 + TSD-023)** está implementada como `AnaliseIaOpenAiAdapter` na branch `backend/ia-openai` (TSD-023 empilhada sobre a TSD-022, commit `c781067` — **as duas entram juntas na `main`**), **aguardando aprovação de merge do usuário + smoke ponta-a-ponta com a `OPENAI_API_KEY` real**. **Frontend TSD-002 + RF-002 + RF-001/004 + RF-010 + RF-007 + RF-009 + RF-008 + RF-011 + RF-017 + RF-014 + RF-012 + RF-016** mergeados — **MVP frontend completo**.
- **RF-016 / TSD-011** (relatório PDF final, backend) — ciclo fechado, 6 papéis, gates cumpridos: `npm run ci` ✅ (106 unit) · `test:e2e` ✅ 39/39 · Crítico ✅. **Mergeado neste commit** (integração com a linha do frontend).
- **A-02 / TSD-022 — integração da IA real (adapter OpenAI da `AnaliseIaPort`, backend)** na branch `backend/ia-openai` (02/09/2026); ciclo completo dos 6 papéis; **Crítico aprovou (condicional)**, não-bloqueadores aplicados e commitados. `AnaliseIaOpenAiAdapter implements AnaliseIaPort` (novo): sobe o PDF pela Files API (`purpose: 'user_data'`), chama `client.responses.create` com `input_file` + lista de requisitos e `text.format` `json_schema` strict, divide em lotes de `IA_MAX_REQUISITOS_POR_CHAMADA` (default **50**), sempre apaga o arquivo no `finally`, mapeia `codigo → requisitoId` e só aceita `paginaReferencia` inteiro `≥ 1`. Factory do `core.module.ts` devolve o adapter OpenAI quando `IA_ADAPTER === 'openai'` (fora de `providers[]`, para o construtor poder exigir a chave sem quebrar o boot em `stub`); bloco `ia` em `configuration.ts`/`env.validation.ts` (`adapter` `stub`|`openai` default `stub`, `modelo` default `gpt-4o`, `openaiApiKey` obrigatória sse `openai`, `baseUrl` opcional, `maxRequisitosPorChamada` default 50); `IA_ADAPTER='http'` agora dá erro apontando para `openai`. Dep nova `openai@^5.23.2` (v7 exige `engines.node>=22`; backend declara `>=20`). Worker `ProcessamentoService` **não mudou**. Gates reais: `npm run ci` ✅ (**126 unit / 21 suites**) · `npm run test:contract` ✅ 2/2 (hoje só contra o stub) · `npm run test:e2e` ✅ 39/39 (não-regressão, `IA_ADAPTER` não setado → stub). **Pendente:** aprovação de merge do usuário + **smoke ponta-a-ponta com a `OPENAI_API_KEY` real** (até lá: "implementada, aguardando smoke real"; plano B = `chat.completions` + `response_format` se a OpenAI recusar a forma do `responses.create` — TSD-022 §9). Desvios registrados na TSD-022 §13 e no `audit.md`.
- **TSD-023 — entrada por texto extraído do PDF (follow-up da TSD-022, A-02, backend)** na mesma branch `backend/ia-openai`, commit `c781067` empilhado sobre a TSD-022 (02/09/2026); ciclo completo dos 6 papéis; **Crítico aprovou (condicional)**, sem bloqueadores de Dev. Motivada pelo smoke real de 02/09 que recusou um edital de 142 páginas / 13,8 MB com `400 input exceeds the context window`. Só o interior do `AnaliseIaOpenAiAdapter` mudou: `analisar()` vira despachante e decide **1×/análise** — **caminho texto (primário):** extrai o texto do PDF **por página** (`pdfjs-dist@3.11.174`, build `legacy/build/pdf.js` CJS, best-effort), monta blocos `=== Página N ===` e manda como `input_text`, **sem `files.create`**; **caminho arquivo (fallback):** corpo da TSD-022 (`files.create`/`input_file`/`files.delete`) **inalterado**, disparado quando o texto é insuficiente, com `logger.warn`. Critério do fallback em constantes fixas (**sem env var**): `MIN_CHARS_PAGINA_COM_TEXTO=20`, `MIN_PAGINAS_COM_TEXTO=1`, `MIN_MEDIA_CHARS_POR_PAGINA=100`. Helper `mapearResposta()` compartilhado; `SCHEMA_SAIDA`/`max_output_tokens` inalterados; dois prompts (`PROMPT_SISTEMA_ARQUIVO` = TSD-022 verbatim, `PROMPT_SISTEMA_TEXTO` novo). **Contrato `AnaliseIaPort`, worker, `configuration.ts`, `env.validation.ts`, schema Prisma, controllers, `.env.example` — não tocados.** Dep nova de runtime `pdfjs-dist: "3.11.174"` (versão exata, sem `^`). Spec unit 10 → 21 casos, `pdfjs-dist/legacy/build/pdf.js` mockado. Gates reais: `npm run ci` ✅ **exit 0** (lint/`tsc`/`prisma validate` ok, **jest unit 138 passed / 21 suites / 0 skipped**, `nest build` ok) · `npm run test:contract` ✅ 2/2 · `npm run test:e2e` ✅ 39/39 (não-regressão, `IA_ADAPTER` ausente → stub). `npm audit` (não é gate): a dep nova traz a **CVE-2024-4367** (`pdfjs-dist <=4.1.392`, high) — mitigada no `getDocument` (`isEvalSupported: false` + `disableFontFace`/`useSystemFonts: false` + o adapter só extrai texto, nunca rasteriza) e **aceita pelo usuário (02/09/2026) como dívida de segurança rastreada**; follow-up = migrar para pdfjs v4+/`unpdf` quando o `ts-jest` do backend for para ESM e re-rodar `npm audit`. Transitivas `tar` (critical) / `@mapbox/node-pre-gyp` (high) são deps **opcionais de `canvas`, NÃO instaladas** (só no lockfile). Warning novo só no e2e (`Cannot polyfill DOMMatrix ... Cannot find module 'canvas'`) — inócuo, não afeta resultado. **Pendente:** merge (junto com a TSD-022) + **smoke real** com o edital de 142 páginas + 1 PDF escaneado (registrar em checkpoint/audit quando a `OPENAI_API_KEY` estiver disponível). Desvios 1–6 na TSD-023 §13 e no `audit.md`.
- **Frontend (`frontend/`) — TSD-002 + RF-002/TSD-011 + RF-001+RF-004/TSD-012 mergeados na `main`** (01/09/2026). App Next.js 15: `/` listagem real (tabela + busca/filtro/ordenação/paginação na URL + estados vazio/carregando/erro) e modal "Nova análise" (NUP/objeto/upload de PDF, validação client-side, `POST /analises` multipart). Camada de dados atrás de seam (`getAnalisesGateway` — fixtures ou `HttpAnalisesGateway` por `NEXT_PUBLIC_API_BASE_URL`); testes de contrato de `GET /analises` e `POST /analises`.
- **Frontend — RF-010 / TSD-013 (tela de análise)** mergeada na `main` (`28c9e8c`). `/analise/[id]` deixa de ser casca: `AnalisesGateway.abrirAnalise` (`GET /analises/:id`, contrato TSD-007) + `AnaliseHeader` + `PainelRevisao` (abas Checklist/Técnica com navegação livre, progresso do `resumo`, lista de requisitos por área em acordeão **somente leitura**) + `AutoRefreshAnalise` (polling enquanto `PENDENTE`/`PROCESSANDO`) + `not-found.tsx`. Decisões validadas: Técnica **fiel ao backend** (badge de status; diverge do protótipo — P-04), checkbox de "verificado" desabilitado (RF-011).
- **Frontend — RF-007 / TSD-014 (status sugerido pela IA)** mergeada na `main` (`cbe9f5b`; branch `frontend/rf-007-status-ia` empurrada). Slice de apresentação sobre o `RequisitoItem`: marca "IA" quando `statusFinal === statusSugeridoIa`; chip "IA: <sugestão>" + realce âmbar + bloco "Sugestão da IA / Parecer atual" quando diverge; legenda no topo da lista. Helper `divergeDaIa`. Sem mudança de contrato/tipo. O caminho "diverge" só ocorre de verdade com RF-008 — testado via fixture forjada (`fixtures-analise-detalhe.ts`, item `av-6`).
- **Frontend — RF-009 / TSD-015 (filtros por status + visão inicial priorizando não conformes)** mergeada na `main` (`2e8f6d5`; branch `frontend/rf-009-filtros` empurrada). Slice de apresentação sobre o `PainelRevisao`: chips **Não conforme (padrão) / Conforme / Não se aplica / Todos** (adiados do RF-010), filtro por `statusFinal`, compartilhado entre abas e **espelhado na URL** (`?requisitos=<slug>`, omitido no padrão); grupos sem itens no filtro seguem visíveis com placeholder; estado "nenhum não conforme" (PRD §9) + atalho "Ver todos os requisitos". `filtro-requisito.ts` puro. Fixture id "7" sem não conformes (andaime) para exercitar o §9. Sem mudança de contrato/tipo.
- **backend — CORS habilitado** (`35ee46d`, branch `backend/cors`): `main.ts` `app.enableCors({ origin: corsOrigins.length ? corsOrigins : true, methods: [...] })`; `configuration.ts` `corsOrigins` (lista de `CORS_ORIGINS`) + `parseCorsOrigins` + unit. Mudança de infra pedida pelo responsável do frontend para destravar o smoke frontend↔backend (os modais chamam a API do navegador). `npm run ci` do backend verde (110 unit); `test:e2e` não roda nesta máquina (Postgres embutido recusa sob usuário admin) — sem relação com a mudança, e o gate `ci` do backend não inclui e2e.
- **Frontend — RF-012 / TSD-020 (botão "Concluir análise" + modal de confirmação)** na branch `frontend/rf-012-conclusao` (02/09/2026); **Crítico aprovou**, sem bloqueadores; `npm run ci` verde (**240 testes / 26 arquivos**). `AnalisesGateway.concluirAnalise(id)` → `AnaliseDetalhe` (`POST /analises/:id/concluir` sem corpo — Http + Fixtures + contrato); `AnaliseRequisitosPendentesError` (`422`, com `.pendentes: RequisitoPendente[]`; degrada p/ `[]` se o backend não trouxer a lista); `409` → `AnaliseConflitoError`, `404` → `AnaliseNaoEncontradaError`. `ConcluirAnalise` (client, novo) — botão no `AnaliseHeader` via nova prop `acao?: ReactNode`, `disabled` enquanto `resumo.obrigatoriosPendentes > 0` + motivo ligado por `aria-describedby`; modal `createPortal` seguindo o `CompletionModal` do protótipo (card central, círculo `--color-brand-muted` + `CircleCheckIcon`, `<h2>`, `<p>`, Cancelar/Concluir), A11y reaproveitada do `AlterarParecerModal`; `422` lista `codigo — titulo` e mantém aberto. `TelaAnalise` passa a orquestrar `detalheEfetivo` (trocado na conclusão, **sem `router.refresh()`**) + `resumoAtual` (espelhado do `PainelRevisao` via nova prop `onResumoChange`), e o `AnaliseHeader` migrou do `page.tsx` para dentro dela. **Smoke conjunto contra o NestJS real** (Postgres 17, `:3000` / frontend `:3201`) feito e registrado em `frontend/docs/visual-reference/rf-012/`: botão bloqueado (11 pendentes) → verificar o último pela UI destrava sem reload → corrida real `422` com a lista → re-verificar → `200` → tela `CONCLUIDA` read-only, `status`/`concluidaEm` confirmados no banco. Sem mudança de contrato. Aguardando push da branch + merge.
- **Frontend — RF-016 / TSD-021 (botão "Baixar relatório" na análise concluída)** na branch `frontend/rf-016-relatorio` (02/09/2026); **Crítico aprovou**, sem bloqueadores; `npm run ci` verde (**250 testes / 26 arquivos**). **Último RF do MVP frontend.** `AnalisesGateway.urlRelatorio(analiseId): string | null` — `Http` monta `${base}/analises/${id}/relatorio` (id encodado); `Fixtures` → `null`. Segundo uso do padrão "seam expõe URL de binário", ao lado do `urlPdf` do RF-014 (URL de navegação, não `fetch`). `BaixarRelatorio` (client, novo) — `url` → `<a href target="_blank" rel="noopener noreferrer">Baixar relatório</a>` (estilo do botão do RF-012); `null` → span desabilitado + aviso "Disponível com a tela conectada ao backend.". `TelaAnalise.acao` ganha o ramo `status === "CONCLUIDA"` → `<BaixarRelatorio>` (o slot do "Concluir análise"); concluir pelo modal do RF-012 troca o botão sem reload. `AnaliseHeader` intocado. **Smoke conjunto contra o NestJS real** (`:3000` / `:3201`): análise `CONCLUIDA` → botão "Baixar relatório" → nova aba com `GET /analises/:id/relatorio` → `200 application/pdf` `inline`, o visor nativo renderiza o relatório; análise `PRONTA` → "Concluir análise", sem "Baixar relatório"; `GET /relatorio` não `CONCLUIDA` → `409` (confirmado direto na API); fixtures → botão desabilitado + aviso. Evidência: `frontend/docs/visual-reference/rf-016/`. Sem mudança de contrato. Aguardando push da branch + merge.
- **Frontend — RF-014 / TSD-019 (visor de PDF + referência de página clicável + editor)** na branch `frontend/rf-014-visor` (02/09/2026); **Crítico aprovou**; `npm run ci` verde (**214 testes / 24 arquivos**). `AnaliseVisor` (novo) — com `NEXT_PUBLIC_API_BASE_URL`, um `<iframe>` do PDF inteiro (`GET /analises/:id/pdf`) navegado por `#page=N` + toolbar ‹ [nº] de total ›; sem backend, um aviso + o total de páginas. `TelaAnalise` (novo, client) segura o estado `pagina` compartilhado entre visor e painel (o `<Suspense>` migrou do `page.tsx`); `AnaliseVisorPlaceholder` removido. `RequisitoItem` — "Página N" vira link que move o visor (`onIrParaPagina`) e ganha um editor inline de página → `PATCH { paginaReferencia }` via `AnalisesGateway.corrigirPaginaReferencia` (reusa `patchRevisao`; não dispara a R-06); `AnalisesGateway.urlPdf(id)` → `string|null`. `pagina-referencia.ts` puro. **Smoke conjunto** feito contra um backend **stub** (contrato do Pablo + PDF real de 3 páginas) — o `<iframe>` renderiza e o clique na referência navega para `#page=2`; o e2e contra o NestJS real não roda nesta máquina (sem PostgreSQL) e fica com o responsável. Sem mudança de contrato. Aguardando push da branch + merge.
- **Frontend — RF-017 / TSD-018 (justificativa visível + erro de revisão fiel)** mergeada na `main` (`bbbf7b8`; branch `frontend/rf-017-comentario` empurrada); **Crítico aprovou**; `npm run ci` verde (**186 testes / 21 arquivos**). Slice de **acabamento** — a _exigência_ de comentário nas ações de revisão já vinha do RF-008 (modal sempre pede) + backend (R-06 no `PATCH`). Slice de **acabamento** — a _exigência_ de comentário nas ações de revisão já vinha do RF-008 (modal sempre pede) + backend (R-06 no `PATCH`). `StatusIaResumo` passa a mostrar "Justificativa da alteração: <comentario>" quando `statusFinal ≠ statusSugeridoIa`; a linha "Comentário:" do `RequisitoItem` fica só para itens não divergentes (sem duplicar); o `catch` do toggle de "verificado" mostra a mensagem do backend no `422` (`AnaliseValidacaoError`) em vez da genérica. Sem componente novo, sem mudança de contrato. Aguardando push da branch + merge.
- **Frontend — RF-011 / TSD-017 (checkbox de "verificado" interativo)** mergeada na `main` (`5fc5730`; branch `frontend/rf-011-verificado` empurrada); **Crítico aprovou**; `npm run ci` verde (**179 testes / 20 arquivos**). Segundo consumidor do `PATCH` da TSD-008: `AnalisesGateway.marcarVerificado(analiseId, requisitoId, verificado)` → `{ item, resumo }` (Http com helper `patchRevisao` comum ao `revisarRequisito`; Fixtures aplica só o `verificado`, **não persiste**). `RequisitoItem` — o checkbox perde `disabled`/`readOnly` quando vem `onToggleVerificado?`; estado local `salvando` (trava durante o PATCH) + `erroToggle` (`role="alert"` no item, reverte o valor). `PainelRevisao` só passa o fio quando `podeEditar`; progresso e `X/Y verificados` já saíam de `resumo` de estado (RF-008) → acompanham. Sem "verificar tudo", sem conclusão (RF-012). Fixtures **não persistem** (andaime). Sem mudança de contrato. Aguardando push da branch + merge.
- **Frontend — RF-008 / TSD-016 (modal "Alterar parecer")** mergeada na `main` (branch `frontend/rf-008-parecer` empurrada; 01/09/2026); **Crítico aprovou**; `npm run ci` verde (**164 testes / 20 arquivos**). Primeira **escrita** da tela de análise: `AnalisesGateway.revisarRequisito` (`PATCH /analises/:id/requisitos/:requisitoId`, contrato TSD-008 — Http + Fixtures + contrato; `AnaliseConflitoError` 409); `alterar-parecer.ts` puro (`validarAlteracaoParecer`/`resolverAlteracaoParecer`); `AlterarParecerModal` (portal — parecer atual read-only + select sem o status atual + comentário obrigatório; dica quando diverge da IA; erros 422/409/404/rede no modal); `RequisitoItem` ganha o botão "Alterar parecer" no bloco expandido (só quando `PRONTA_PARA_REVISAO`); `PainelRevisao` vira _stateful_ — `overrides: Map<idAvaliacao, item>` + `resumo` de estado aplicados antes de `separarPorAba`/`filtrarPorStatus`, zerados só ao trocar de análise. Sem `verificado` manual (RF-011) nem página (RF-014). Fixtures **não persistem** (andaime). Sem mudança de contrato.
- Ciclos de frontend rodam o ciclo completo de seis papéis (PM→Engenheiro→Dev→Testes→**Crítico**→Documentador); a User Story e a TSD são validadas pelo responsável antes de implementar, e o `critico` roda como passo próprio a partir do RF-007.
- Infra de teste: `test:e2e` sobe/derruba um PostgreSQL embutido (`embedded-postgres`) — sem Docker.
- Integração com o serviço de IA real foi adiada para o fim do MVP por decisão do responsável (31/08/2026); os ciclos rodaram sobre o `AnaliseIaStubAdapter`. **Agora implementada** como `AnaliseIaOpenAiAdapter` (A-02 / TSD-022 + TSD-023, branch `backend/ia-openai`) — **não** como `HttpAdapter` genérico: o adapter fala com a OpenAI/GPT pelo SDK `openai`, selecionado por `IA_ADAPTER=openai` (o `stub` segue o padrão). A entrada ao modelo é o **texto do PDF por página** (marcadores `=== Página N ===`); PDF sem texto aproveitável cai automaticamente no envio do arquivo via Files API (TSD-023). Falta o merge + o smoke real com a chave.
- **Modelo de branch:** uma branch curta por ciclo/TSD, merge na `main` quando o Documentador fecha.

## 2. Spec ativa

- **Nenhuma spec ativa — o MVP de features está completo nas duas frentes.**
- **Backend:** RF-016 / TSD-011 (relatório PDF) mergeada — **features de backend do MVP completas**. A **integração da IA real** (A-02) foi implementada em **TSD-022** (adapter OpenAI da `AnaliseIaPort`) + **TSD-023** (entrada por texto extraído do PDF por página, com fallback automático para a Files API — follow-up motivado pelo smoke real que estourou o contexto num edital de 142 páginas), na branch `backend/ia-openai` (TSD-023 = commit `c781067`, empilhado sobre a TSD-022; **as duas mergeiam juntas**): Crítico aprovou as duas (condicional), não-bloqueadores aplicados. Gates da TSD-023: `npm run ci` **exit 0** (jest unit **138 passed / 21 suites**), `test:contract` 2/2, `test:e2e` 39/39. **Aguarda aprovação de merge do usuário + smoke ponta-a-ponta com a `OPENAI_API_KEY` real** — edital de 142 páginas + 1 PDF escaneado (a chave será fornecida pelo responsável). Até lá: "implementada, aguardando smoke real". `npm audit` acusa a **CVE-2024-4367** (`pdfjs-dist@3`, high) — mitigada e **aceita pelo usuário como dívida rastreada** (02/09/2026), follow-up = migração para pdfjs v4+/`unpdf` após o `ts-jest` ir para ESM.
- **Frontend:** RF-016 / TSD-021 ("Baixar relatório") — **último RF do MVP frontend** — implementada, Crítico ✅ (sem bloqueadores), `npm run ci` verde (250 testes), smoke conjunto contra o NestJS real feito, na branch `frontend/rf-016-relatorio`. RF-012 / TSD-020 na branch `frontend/rf-012-conclusao` no mesmo estado. **As duas aguardam só push + merge na `main`.** Não há próximo RF de frontend — só os follow-ups de acabamento do §5.
- Os pontos de aprovação PM→Engenheiro e Engenheiro→Dev são paradas explícitas (ver observação no §7). No frontend, o responsável valida a User Story e a TSD antes de qualquer implementação de RF, e cada RF concluído é mergeado e empurrado incrementalmente.

## 3. Specs concluídas

| Spec                                                                 | Resultado                                                                                                                                                                                                                                                                                                                                                                                                                    | Validação                                                                                                                                                                                                                                                                                |
| -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TSD-001 — Fundação técnica do backend                                | Projeto NestJS + Prisma + PostgreSQL em `backend/`; config com validação de boot; `/health`; 4 módulos de feature vazios; 3 portas (`AnaliseIaPort`, `ArmazenamentoPdfPort`, `AnalistaAtualProvider`) com adapter mínimo; PostgreSQL embutido no `test:e2e`. Sem código de produto.                                                                                                                                          | `npm run ci` ✅ (lint, typecheck, prisma validate, test 23/23, build) · `npm run test:e2e` ✅ 5/5 · Crítico ✅ · mergeada `main` `13b111e`                                                                                                                                               |
| TSD-003 — Base fixa de requisitos (RF-006, backend)                  | Modelo `Requisito` + migration; importador de CSV externo com imutabilidade de texto/`obrigatorio` e abort transacional; `RequisitosService.listarAtivos()`; seed placeholder (12 itens). Sem endpoint HTTP.                                                                                                                                                                                                                 | Unit 23/23 ✅ · integração `requisitos-importador` ✅ (idempotência, rollback, ordenação) · Crítico ✅ · mergeada `main` `13b111e`                                                                                                                                                       |
| TSD-004 — Criar análise com PDF persistido (RF-001/004/018, backend) | Modelo `Analise` + migration; `POST /analises` (multipart) com validação de PDF (mimetype + `%PDF-` + limite 25 MB + recusa `/Encrypt`); `GET /analises/:id` e `/:id/pdf`; `status` string+allowlist (só `PENDENTE`); 422 sem criar linha, 502 se falha ao gravar PDF. Sem listagem/worker/avaliação.                                                                                                                        | `npm run ci` ✅ (38 unit) · `npm run test:e2e` ✅ 9/9 (+ integração `analises`: criar→buscar→baixar PDF, 422, 404) · build ✅ · Crítico ✅ · mergeada `main` `9dd6792`                                                                                                                   |
| TSD-005 — Listagem de análises (RF-002, backend)                     | `GET /analises` com `q` (nup+objeto, insensitive), `status` (multi: vírgula ou repetição), `ordenarPor` (iniciadaEm\|nup), `ordem`, `pagina`/`tamanho` (20, máx 100) → `{ itens, total, pagina, tamanho }`; itens com 6 campos; query inválida → 422. Sem mudança de modelo.                                                                                                                                                 | `npm run ci` ✅ (49 unit) · `npm run test:e2e` ✅ 16/16 (+ integração `listagem-analises`: escopo por analista, q, status multi, ordenação, paginação, 422, vazio) · build ✅ · Crítico ✅ · mergeada `main` `57b170f`                                                                   |
| TSD-006 — Processamento da análise (RF-005/RF-007, backend)          | Modelo `AvaliacaoRequisito` + migration; `ProcessamentoService` (claim atômico, timeout IA, gravação transacional de 1 avaliação/requisito, `PRONTA_PARA_REVISAO`/`ERRO_PROCESSAMENTO`); disparo imediato no `criar` + varredura + `recuperarPresas` no boot sob `PROCESSAMENTO_AUTO`; `POST /analises/:id/reprocessar` (404/409). Sem `@nestjs/schedule` (ESM).                                                             | `npm run ci` ✅ (61 unit) · `npm run test:e2e` ✅ 21/21 (+ integração `processamento`: processar, claim atômico, base vazia→erro, reprocessar 200/409/404, recuperar) · build ✅ · Crítico ✅ · mergeada `main` `67ff89f`                                                                |
| TSD-007 — Abrir análise (RF-009, backend)                            | `GET /analises/:id` passa a devolver `resumo` (contagens por `statusFinal` + verificados + obrigatórios pendentes) e `avaliacoesPorArea` (grupos por área alfabética; dentro, `NAO_CONFORME` primeiro depois `ordem`); item com `norma` estruturada. Sem mudança de modelo; contrato anterior preservado.                                                                                                                    | `npm run ci` ✅ (65 unit) · `npm run test:e2e` ✅ 24/24 (+ integração `abrir-analise`: processar→abrir→agrupamento/ordem/resumo, não processada→vazio, 404) · build ✅ · Crítico ✅ · mergeada `main` `96ff8fb`                                                                          |
| TSD-008 — Revisão de requisito (RF-008/011/017, backend)             | `PATCH /analises/:id/requisitos/:requisitoId` (`statusFinal`/`verificado`/`comentario`); alterar `statusFinal` ⇒ `verificado=true`; comentário obrigatório sse resultado `statusFinal ≠ statusSugeridoIa` sem comentário (R-06); `409` fora de `PRONTA_PARA_REVISAO`, `404`, `422`; resposta `{ item, resumo }`. Sem mudança de modelo.                                                                                      | `npm run ci` ✅ (75 unit) · `npm run test:e2e` ✅ 28/28 (+ integração `revisao-requisito`: 422 sem comentário / 200 com / verificado sem comentário / 422 inválido+vazio / 404 / 409) · build ✅ · Crítico ✅ · mergeada `main` `ad2e8cd`                                                |
| TSD-009 — Referência de página do PDF (RF-014, backend)              | `Analise.totalPaginasPdf Int?` + migration; contagem best-effort com `pdf-lib` no `criar` (`null` se não parseável, sem falhar); `totalPaginasPdf` no `GET /analises/:id`; `PATCH .../requisitos/:requisitoId` aceita `paginaReferencia` (`1..total` \| `≥1` \| `null` → senão `422`), **sem** disparar a R-06; `lerPagina` segue como seam.                                                                                 | `npm run ci` ✅ (91 unit) · `npm run test:e2e` ✅ 32/32 (+ integração: criar→`totalPaginasPdf` no payload / `null` quando não parseável / `PATCH paginaReferencia` ok / fora do intervalo → 422 / `null` limpa / total desconhecido) · build ✅ · Crítico ✅ · mergeada `main` `19f308b` |
| TSD-010 — Conclusão da análise (RF-012/013/015, backend)             | `POST /analises/:id/concluir` (`@HttpCode 200`): `404` \| `CONCLUIDA` → `200` idempotente \| fora de `PRONTA_PARA_REVISAO` → `409` \| obrigatório não verificado → `422` com `requisitosPendentes` \| senão transição atômica `updateMany` → `CONCLUIDA` + `concluidaEm`, devolve payload completo. `AnaliseDetalhe` ganha `analistaId` + `analistaNome` (RF-013). Trava só por `verificado`. Sem migration.                 | `npm run ci` ✅ (97 unit) · `npm run test:e2e` ✅ 36/36 (+ integração `conclusao-analise`: 422+lista / 200+CONCLUIDA+concluidaEm+responsável / idempotente / não-obrigatório não bloqueia / 409 / 404) · build ✅ · Crítico ✅ · mergeada `main` `56f83bd`                               |
| TSD-011 — Relatório PDF final (RF-016, backend)                      | `GET /analises/:id/relatorio` → `application/pdf` `inline`, sob demanda com `pdfkit`, **não persiste** (A-04). `409` se `!= CONCLUIDA`; `404`. `RelatorioModule` novo (consome `AnalisesService`); `montarModeloRelatorio` puro (itens por `ordem`, `formatarNorma`) + `renderRelatorioPdf`. Conteúdo: NUP, objeto, responsável, datas, resumo, e por requisito `codigo`/`titulo`/norma/página/`statusFinal`. Sem migration. | `npm run ci` ✅ (106 unit) · `npm run test:e2e` ✅ 39/39 (+ integração `relatorio-pdf`: `200 application/pdf inline` + `%PDF-` / `409` antes de concluir / `404`) · build ✅ · Crítico ✅ · **branch `backend/rf-016-relatorio`, aguardando merge**                                      |

### 3.1 Notas de revisão dos ciclos fechados

**Crítico — A-02 / TSD-022 (backend, 02/09/2026):** **APROVADO (condicional)**. O adapter OpenAI implementa a `AnaliseIaPort` sem tocar no worker/porta/schema/REST; unit com o SDK mockado cobre request (`input_file` + lista + `text.format` json_schema strict), parse, mapeamento por `codigo`, descarte de código/status inválido, lotes contra o mesmo arquivo, `files.delete` no `finally` mesmo em erro, propagação de erro do SDK e resposta `incomplete`. Não-bloqueadores exigidos e **já aplicados/commitados**: `IA_MAX_REQUISITOS_POR_CHAMADA` default 200 → **50** (margem de `max_output_tokens` por lote); `FixturesAnalisesGateway.urlPdf` assinou `(_id: string)` para casar a interface (commit `e3999fe`, arrasto do RF-014). Pendência que o Crítico não pôde fechar: **o smoke ponta-a-ponta com a OpenAI real** (sem a chave). Desvios de implementação vs. TSD (`files.delete` vs `files.del`; `openai@^5.23.2` vs `^7`; `analise-ia.contract.spec.ts`) aceitos — registrados na TSD-022 §13.

**Documentador (02/09/2026):** ciclo **A-02 / TSD-022** (backend) **fechado** — Crítico ✅ (condicional), não-bloqueadores aplicados e commitados na branch `backend/ia-openai`. Gates reais: `npm run ci` ✅ (126 unit / 21 suites) · `npm run test:contract` ✅ 2/2 · `npm run test:e2e` ✅ 39/39. TSD-022 → `status: implementada`, §8 marcado (com o smoke real explicitamente desmarcado e anotado), §13 (nota de fechamento + desvios 1–4). `questoes-abertas.md`: **A-02 resolvida** → Resolvidas **R-07** (contrato fechado: aciona a OpenAI via SDK, entrada PDF + requisitos, saída sugestão por requisito + `paginaReferencia`, timeout reusa `IA_TIMEOUT_MS` via `comTimeout`, sem retry próprio, adapter por `IA_ADAPTER`); **P-05** segue parcial (a sugestão de página existe no adapter, confirma-se só no smoke). SDD §2/§6 (diagrama + tabela de componentes)/§8 (`AnaliseIaPort` interno)/§9 (decisão + checklist + questões) alinhados ao `AnaliseIaOpenAiAdapter`. Roadmap: seção "Fora da tabela — Integração da IA real (A-02)" → Status (backend) `implementada na branch backend/ia-openai — aguardando merge + smoke real`, TSD associada `implementada`, "Notas do ciclo (backend — 02/09/2026)". Detalhe em `.ai-dev/audit.md` (`02/09/2026`). **Aguardando aprovação de merge do usuário + smoke ponta-a-ponta com a `OPENAI_API_KEY` real.** Pendência paralela: decidir se `backend/scripts/*` + `.claude/launch.json` entram no versionamento.

**Crítico — TSD-023 (backend, 02/09/2026):** **APROVADO (condicional)**, sem bloqueadores de Dev. O `AnaliseIaOpenAiAdapter` vira despachante texto-primário / Files-API-fallback sem tocar no contrato da `AnaliseIaPort`, no worker, na config, no schema nem no REST; spec unit 10 → 21 casos (`pdfjs-dist/legacy/build/pdf.js` mockado) cobre caminho texto sem `files.create`, fallback com `warn` + `input_file`, critério de fallback no limiar, `paginaReferencia` do marcador, chunking com texto repetido e zero `files.create`, erros JSON/`incomplete`/SDK no caminho texto, construtor sem `OPENAI_API_KEY`. Condição do Crítico: aceitar formalmente a **CVE-2024-4367** (pdfjs v3, high) — o usuário aceitou como dívida rastreada em 02/09/2026 com a mitigação aplicada (`isEvalSupported: false` + hardening de fonte + sem rendering). Pendência que o Crítico não pôde fechar: **o smoke ponta-a-ponta com a OpenAI real** (edital de 142 páginas + PDF escaneado). Desvios 1–6 registrados na TSD-023 §13.

**Documentador (02/09/2026) — TSD-023:** ciclo **TSD-023** (follow-up da TSD-022, A-02, backend) **fechado** — Crítico ✅ (condicional), sem bloqueadores de Dev, na branch `backend/ia-openai` (commit `c781067`, empilhado sobre a TSD-022 — **mergeiam juntas**). Gates reais: `npm run ci` **exit 0** (lint/`tsc`/`prisma validate` ok, **jest unit 138 passed / 21 suites / 0 skipped**, `nest build` ok) · `npm run test:contract` ✅ 2/2 · `npm run test:e2e` ✅ 39/39 (não-regressão, `IA_ADAPTER` ausente → stub). TSD-023 → `status: implementada`, §8 marcado item a item com evidência (smoke real **explicitamente desmarcado** e anotado "implementada, aguardando smoke real"; "SDD/TSD-022 §9" feitos), §13 nova (nota de fechamento + desvios 1–6). SDD §2/§4/§6 (tabela de componentes) / §8 (`AnaliseIaPort` interno) / §9 (nova linha na tabela de decisões) / §12 alinhados a "texto por página + fallback Files API". `022-integracao-ia-openai.tsd.md` §9 ganhou o ponteiro para a TSD-023. `questoes-abertas.md`: **P-05** atualizada (a origem da página mudou — marcador `=== Página N ===` do texto extraído; segue parcial, confirma no smoke); **R-07** anotada (entrada agora é texto por página, Files API virou fallback); **nova A-08** (valor definitivo de `MIN_MEDIA_CHARS_POR_PAGINA` + editais > 128k tokens mesmo como texto — decidir se o smoke pedir); **nova A-09** (dívida de segurança CVE-2024-4367 — mitigada, aceita, follow-up de migração). Roadmap: subseção "Follow-up: entrada por texto extraído (backend)" → Status `implementada na branch backend/ia-openai — aguardando merge + smoke real`, TSD associada `implementada`, "Notas do ciclo (backend — 02/09/2026)". Detalhe em `.ai-dev/audit.md` (`02/09/2026`). **Pendências:** merge (junto com a TSD-022); **smoke ponta-a-ponta com a `OPENAI_API_KEY` real** (edital de 142 páginas + 1 PDF escaneado — registrar em checkpoint/audit); migração `pdfjs-dist` v4+/`unpdf` após o `ts-jest` ir para ESM (+ re-`npm audit`); decisão de versionar `backend/scripts/*` + `.claude/launch.json` (aberta do ciclo anterior).

**Crítico — RF-016 / TSD-021 (frontend, 02/09/2026):** **APROVADO**, sem bloqueadores. Escopo §3 item a item (interface `urlRelatorio` + `Http` monta `${base}/analises/${id}/relatorio` idêntico ao `urlPdf` + `Fixtures` → `null`; `BaixarRelatorio` reproduz o texto da §3 — `<a target="_blank" rel="noopener noreferrer">` / `<span aria-disabled>` + `role="note"`; `AnaliseHeader` intocado; `TelaAnalise.acao` vira ternário de 3 casos usando `detalheEfetivo`). Nenhuma decisão fora da TSD. Decisões §9 respeitadas (link em nova aba, validada pelo usuário; botão só em `CONCLUIDA`; sem backend → desabilitado + aviso; `urlRelatorio` no data layer). §7.1 conferida linha a linha: unit dos dois ramos por _role_ real; troca sem reload testada dentro do fluxo do modal RF-012; contrato monta a URL com id encodado e sem barra dupla; **smoke conjunto realmente executado** (não mock) — `CONCLUIDA` → nova aba `GET /analises/:id/relatorio` `200 application/pdf inline` `%PDF-`; `PRONTA` sem o botão; `409` em não-`CONCLUIDA` confirmado direto na API; fixtures → desabilitado. Seam respeitado (nenhum componente monta a URL). Sem regressão RF-012/014 (`AnaliseHeader` intocado; ramo `PRONTA` do `acao` idêntico; teste de conclusão do RF-012 estendido, não reescrito). A11y coerente com o precedente (RF-014/RF-012). Ajuste aplicado após o Crítico: teste parametrizado dos estados `PENDENTE`/`PROCESSANDO`/`ERRO_PROCESSAMENTO` sem botão (fecha o §8 item 2). Não-bloqueador de follow-up: A11y unificada nos três botões desabilitados (`BaixarRelatorio` / `ConcluirAnalise` travado / `AnaliseVisor` sem backend) — `aria-describedby` ligando o pseudo-botão ao aviso; fora do escopo da slice.

**Documentador (02/09/2026):** ciclo RF-016 / TSD-021 (frontend) **fechado** — Crítico ✅, `npm run ci` verde (250 testes / 26 arquivos), smoke conjunto contra o NestJS real registrado (`frontend/docs/visual-reference/rf-016/` — 3 screenshots + README). Roadmap: linha 12 da tabela → `implementada — Crítico ✅`; seção RF-016 → Status (frontend) `implementada` + "Notas do ciclo (frontend — 02/09/2026)"; **marco "MVP frontend completo" registrado** (roadmap após a seção RF-016, e §1 deste checkpoint). TSD-021 → `status: implementada`, §8 todos os itens marcados, §10 REF-11 inspecionado. Sem mudança de contrato, sem `questoes-abertas` novas. Detalhe em `.ai-dev/audit.md` (`02/09/2026`). **Aguardando push da branch `frontend/rf-016-relatorio` + merge na `main`.** Com RF-012 e RF-016 mergeados, **o MVP frontend fica completo** — não há próximo RF de frontend.

**Crítico — RF-012 / TSD-020 (frontend, 02/09/2026):** **APROVADO**, sem bloqueadores. Escopo §3 completo e fiel (gateway `concluirAnalise` nas 2 impls + contrato + `index.ts`; `AnaliseRequisitosPendentesError` com `.pendentes`; `ConcluirAnalise` + CSS; `acao?` no `AnaliseHeader`; `onResumoChange` no `PainelRevisao` — mount + `aplicarRevisao`; `TelaAnalise` com `detalheEfetivo`/`resumoAtual`, header migrado do `page.tsx`). Decisões §9 todas implementadas (botão no cabeçalho; gate pelo `resumo` da sessão; conclusão em estado de client, sem `router.refresh()`; sem toast; relatório 100% adiado p/ RF-016; modal de alta fidelidade ao `CompletionModal`). Contrato TSD-010 conferido campo a campo contra `backend/src/analises` (`POST` sem corpo; `422 { requisitosPendentes:[{requisitoId,codigo,titulo,area}] }` → `.pendentes`; `409`/`404`; sucesso → `validarAnaliseDetalhe`). Seam de dados respeitado. Estratégia §7.1 conferida linha a linha, incluindo o **smoke conjunto realmente executado** contra o NestJS real (não substituído por mock). Sem regressão RF-008/009/010/011/014/017 (mudanças aditivas/opcionais). Ajustes aplicados após o Crítico: foco no "Concluir" num `useEffect([montado])` (o portal não existe no efeito de mount — antes `focus()` rodava com ref `null`); motivo do bloqueio ligado ao `<button>` por `aria-describedby` + teste `toHaveAccessibleDescription`; plural do motivo simplificado; `ConcluirAnalise` recebe `detalheEfetivo.id`. Não-bloqueadores registrados: `focus()` com ref-null é padrão pré-existente também no `AlterarParecerModal` (follow-up para os dois modais); em modo fixtures o caminho feliz "verificar → concluir" não é alcançável (`marcarVerificado` não persiste — ressalva de andaime da §9; o caminho real está no smoke); `aria-describedby` não é anunciado por todos os leitores em `<button disabled>` (mitigado, não crítico).

**Documentador (02/09/2026):** ciclo RF-012 / TSD-020 (frontend) **fechado** — Crítico ✅, `npm run ci` verde (240 testes / 26 arquivos), smoke conjunto contra o NestJS real registrado (`frontend/docs/visual-reference/rf-012/` — 5 screenshots + README). Roadmap: linha 11 da tabela de sequência frontend → `implementada — Crítico ✅`; seção RF-012 → Status (frontend) `implementada` + "Notas do ciclo (frontend — 02/09/2026)". TSD-020 §10 (REF-10 inspecionado) e §9 (modal de alta fidelidade, por instrução do usuário) atualizadas. Sem mudança de contrato, sem `questoes-abertas` novas. Detalhe em `.ai-dev/audit.md` (`02/09/2026`). **Aguardando push da branch `frontend/rf-012-conclusao` + merge na `main`.** Resta só **RF-016** (baixar relatório) para o MVP frontend.

**Crítico — RF-014 / TSD-019 (frontend, 02/09/2026):** **APROVADO**, sem bloqueantes. Visor `<iframe>` nativo + `#page=N` (sem `pdf.js`), referência de página clicável, editor inline de página — nada fora do recorte. Os 8 itens de aceite §8 com código + teste; contrato TSD-009 à letra (`urlPdf` monta a URL certa com `encodeURIComponent`; `corrigirPaginaReferencia` manda o corpo **exatamente** `{ paginaReferencia }` e reusa o mapeamento 422/409/404 do `patchRevisao`; `paginaReferencia` não dispara a R-06). `tela-analise.test` cobre a coordenação visor↔painel (clicar em "Página 8" move o `src` do `<iframe>` de `#page=1` para `#page=8`). `next build` verde; 214 testes; `AnaliseVisorPlaceholder` removido limpo. **Smoke conjunto contra o backend real** feito depois do Crítico (02/09/2026 — a máquina tem PostgreSQL 17 do sistema, só o `@embedded-postgres` não sobe sob admin): NestJS `:3000` + frontend `:3201`; `POST /analises` → processa → `totalPaginasPdf=4`; `GET /analises/:id/pdf` `inline` renderiza no `<iframe>`; `PATCH { paginaReferencia: 3 }` do editor inline grava no banco sem R-06; CORS `OPTIONS 204`. Ajustes após o Crítico: `RequisitoItem` usa a constante `PAGINA_REFERENCIA_VAZIO`; + teste `painel-revisao` (CONCLUIDA: "Página N" clicável, sem "Editar").

**Crítico — RF-017 / TSD-018 (frontend, 01/09/2026):** **APROVADO**, sem bloqueantes. Slice de acabamento — a exigência de comentário já vinha do RF-008 + backend. Faz só o que a TSD declara: `StatusIaResumo` mostra "Justificativa da alteração" quando `divergeDaIa` (= `statusFinal !== statusSugeridoIa`, idêntico à R-06); a linha "Comentário:" do `RequisitoItem` sai do item divergente (sem duplicar o texto); o `catch` do toggle usa `AnaliseValidacaoError.motivos[0]` no `422`, genérica nos demais. Modal do RF-008 intocado; sem componente/contrato novo; sem regressão RF-007/008/011. `next build` verde; 186 testes. Ajustes após o Crítico: `?.trim()` também na condição da linha "Comentário:" (simetria); + caso 404 no toggle → mensagem genérica. Não-bloqueantes: item divergente sem comentário (dado legado hipotético — o invariante do PATCH impede) mostraria nem justificativa nem comentário; o ramo genérico do toggle é testado com 409 e 404 (rede cai no mesmo `instanceof`).

**Crítico — RF-011 / TSD-017 (frontend, 01/09/2026):** **APROVADO**, sem bloqueantes. Checkbox de "verificado" operável: aderente ao escopo §3 e às decisões §9 (aplica no sucesso, não optimistic; estado de toggle local ao `RequisitoItem`; erro inline `role="alert"`; método dedicado `marcarVerificado`; sem "verificar tudo"). Fiel ao contrato TSD-008 — corpo do `PATCH` **exatamente `{ verificado }`** (teste de contrato afirma), `verificado` não dispara a R-06, resposta `{ item, resumo }`. Os 10 itens de aceite §8 com código + teste; gate `PRONTA_PARA_REVISAO` via `podeEditar`; reversão do checkbox no erro (input controlado); `verificado` do modal RF-008 continua marcando (sem regressão). A refatoração `patchRevisao`/`localizarAvaliacao`/`resultadoComItem`/`aplicarRevisao` é limpa e não regride o RF-008. `next build` verde; 179 testes. Ajustes após o Crítico: `aria-label` do checkbox não-editável ("Verificado — <título>", antes dizia "disponível no RF-011"); +422 e falha de rede explícitos no contrato de `marcarVerificado`; redação reconciliada na TSD §8/§9/§10. Não-bloqueantes anotados: N `PATCH` concorrentes em itens distintos substituem o `resumo` por inteiro (fora de ordem possível — limitação aceita, backend real recalcula); estado de erro do toggle não é fotografável com fixtures (coberto no unit).

**Crítico — RF-008 / TSD-016 (frontend, 01/09/2026):** **APROVADO**, sem bloqueantes. Primeira escrita da tela de análise: aderente ao escopo §3 e às decisões §9 (modal manda só `{ statusFinal, comentario }`; select exclui o status atual; comentário sempre obrigatório; `AnaliseConflitoError` 409; aplicação imediata via `overrides` + `resumo` de estado, sem reload; fixtures não persistem). Fiel ao contrato TSD-008 — corpo, resposta `{ item, resumo }`, `verificado=true` vindo do backend, R-06 espelhada. Os 11 itens de aceite §8 com código + teste; a chave do override (`item.id`) e a da URL (`requisitoId`) estão corretamente separadas; `next build` verde; screenshot mostra o item verificado + badge + progresso 2/6→3/6 sobrevivendo à troca de filtro. Ajustes aplicados após o Crítico: modal limpa a faixa de erro do servidor ao editar qualquer campo; + testes de componente para `404` e erro de rede e para o "editar limpa o erro" (164 testes); precisão de redação na TSD §3/§7.1 (contagem de aba é estrutural, não sai do `resumo`; a cadeia é testada por costura, não com o gateway real). Não-bloqueantes anotados: validação da fixture é mais estrita que o backend (exige comentário sempre — inofensivo porque o modal sempre envia); o texto da TSD superestimava a cobertura "com o gateway real" (corrigido).

**Crítico — RF-009 / TSD-015 (frontend, 01/09/2026):** **APROVADO**, sem bloqueantes. Aderente ao escopo §3 e às decisões da slice validadas pelo usuário (§9): filtro persistido na URL, grupos vazios visíveis com placeholder, 3 chips + "Todos", filtro por `statusFinal` compartilhado entre abas, estado "nenhum não conforme" + atalho (PRD §9). Os 11 itens de aceite da §8 com evidência em código **e** teste de comportamento; `next build` renderiza a rota como ƒ dynamic (par searchParams↔URL fora do mock); nenhuma chamada nova ao backend, contrato inalterado. Ajustes aplicados após o Crítico: **removido `contarPorFiltro`** (sem consumidor real — a contagem sai de `contarItens(gruposFiltrados)`); `painel-revisao.test` passou a asserir a ausência do grupo de chips em `PROCESSANDO` e que o badge do cabeçalho de grupo reflete os itens visíveis (129 testes). Não-bloqueantes anotados: a **legenda "sugestões da IA" agora só aparece quando há itens visíveis** (decisão de implementação, não estava na §3); a leitura de `?requisitos=` é só na montagem — back/forward do browser não re-sincroniza os chips (fora do escopo, limitação conhecida); contraste do chip "Não se aplica" ativo usa `--color-border-strong` (token do protótipo) — confirmar AA num momento oportuno; a fixture id "7" sem não conformes é andaime (arquivo já rotulado "substituído pela API real").

**Crítico — RF-007 / TSD-014 (frontend, 01/09/2026):** **APROVADO**, sem bloqueantes. Os 7 critérios de aceite da §8 com evidência no código; testes exercitam comportamento real (`divergeDaIa`, marca "IA" × chip, bloco expandido, legenda com/sem itens e em erro); sem vazamento de escopo de RF-008/009/011/017; divergência do protótipo justificada (protótipo mostra um só status por item; PRD RF-007 + glossário vencem). Não-bloqueantes: a fixture `av-6` que passou a divergir também ganhou `verificado=true` + `comentario` (coerente com o invariante R-06 do backend; anotado aqui); o "realce âmbar" de divergência tem evidência só visual (screenshot), não unitária — aceitável em slice de apresentação; legenda aparece mesmo com a aba ativa vazia (inofensivo). Ajustes aplicados no ciclo após o Crítico: asserção da ausência de legenda em `ERRO_PROCESSAMENTO` e do badge principal seguir o `statusFinal` na divergência (113 testes).

**Crítico — TSD-001 (31/08/2026):** **aprovado**. Escopo cumprido, sem vazamento de escopo de produto. Toda a bateria passa, incluindo `test:e2e` (a pendência de ambiente foi resolvida com PostgreSQL embutido). Observações menores não bloqueantes: leve duplicação de defaults entre `configuration.ts` e `env.validation.ts`; `STATUS_REQUISITO` ainda não usado (seam para RF-007); `package.json#prisma` gera aviso de deprecação (migrar para `prisma.config.ts` em algum momento antes do Prisma 7).

**Crítico — RF-006 / TSD-003 (31/08/2026):** **aprovado**. Todos os critérios de aceite da TSD-003 atendidos, com evidência: modelo + migration, `area` como string com allowlist (não enum), importador de CSV com imutabilidade de `titulo`/`descricao`/`norma*`/`obrigatorio` e abort transacional, `RequisitosService.listarAtivos()`, CSV placeholder (12 itens), sem endpoint HTTP, sem outra tabela. Testes de integração (idempotência, rollback transacional, ordenação) **executados e verdes** contra Postgres embutido. Observações menores: `package.json#prisma` gera aviso de deprecação (follow-up: `prisma.config.ts`); `ImportadorRequisitosService` ainda sem consumidor (seam intencional; o seed usa a função pura); `test/jest-e2e.json` passou a cobrir `test/integration/` (correção alinhada ao `quality-gates.md`); `migration.sql` teve uma linha de warning do Prisma removida (tinha vazado pelo redirect de stdout na autoria).

**Crítico — RF-001/004/018 / TSD-004 (31/08/2026):** **aprovado**. Todos os critérios da TSD-004 com evidência (38 unit + 9 e2e): modelo + migration aplicada, validação de PDF (mimetype/magic bytes/limite/`/Encrypt`), 422 sem criar linha, 502 sem criar linha, `GET`/`GET /pdf` com 404, `status` string+allowlist. Refinação registrada no SDD §7: `POST /analises` responde **201** (não 202 — nada assíncrono na requisição). Observações menores: `STATUS_ANALISE`/`isStatusAnalise` são seam para RF-005 (o `create` grava o literal `'PENDENTE'`); heurística `/Encrypt` e teto de upload (60 MB no multer, 25 MB efetivo na validação) documentados; PDF pode ficar órfão se o `create` falhar após `salvar` (aceito no MVP — `ArmazenamentoPdfPort` não tem `delete`).

**Crítico — RF-002 / TSD-005 (31/08/2026):** **aprovado**, com um feedback aplicado no ciclo: o parser da query passou a aceitar o parâmetro repetido (`?status=A&status=B`) além da vírgula — antes um param repetido causaria 500 em vez de 422. Todos os critérios da TSD-005 com evidência (49 unit + 16 e2e): contrato de query, defaults, 422 sem tocar no banco, paginação com `total` do filtro, escopo por analista, estado vazio. Observação de infra: `test:e2e` passou a rodar `--runInBand` — dois integration specs mutam a mesma tabela `analise` no Postgres compartilhado e, em paralelo, um apagava as linhas do outro.

**Crítico — RF-005/RF-007 / TSD-006 (31/08/2026):** **aprovado**. Todos os critérios da TSD-006 com evidência (61 unit + 21 e2e): migration com FKs/unique, 1 avaliação por requisito ativo, `status_final = status_sugerido_ia`, claim atômico não duplica, IA erro/timeout/PDF ilegível/base vazia → `ERRO_PROCESSAMENTO` sem avaliações, `recuperarPresas`, `reprocessar` 200/409/404, `PROCESSAMENTO_AUTO=false` determiniza os testes. Desvio justificado: **`@nestjs/schedule` descartado** (ESM-only, quebra o ts-jest CJS) → `setInterval` próprio com `onModuleDestroy`. Observações menores não bloqueantes: falha na transação da etapa 6 deixa a análise em `PROCESSANDO` até o próximo boot (a varredura só olha `PENDENTE`) — candidato a "re-selecionar `PROCESSANDO` antigo" num ciclo futuro; `comTimeout` não cancela a chamada subjacente (relevante só com o `HttpAdapter` real).

**Crítico — RF-009 / TSD-007 (31/08/2026):** **aprovado**, sem desvios. Todos os critérios com evidência (65 unit + 24 e2e): agrupamento por área alfabética, `NAO_CONFORME` (por `statusFinal`) primeiro depois `ordem`, `resumo` coerente, `norma` estruturada, análise sem avaliações → grupos vazios + resumo zerado, `404`, contrato anterior preservado. Observações menores: payload cresce com a base real (~300 itens — já em §9); o método do controller ainda se chama `buscar`.

**Crítico — RF-008/011/017 / TSD-008 (31/08/2026):** **aprovado**, sem desvios. Todos os critérios com evidência (75 unit + 28 e2e): invariante P-03 (comentário obrigatório sse `statusFinal ≠ statusSugeridoIa`), alterar status ⇒ verificado, verificado alterna livre, `409`/`404`/`422`, `{ item, resumo }` com o mesmo formato de item de `GET /analises/:id`. Refactor de `analise-detalhe.ts` (exporta `toItem`, extrai `calcularResumo`) limpo. Menores: uma avaliação divergente da IA não pode ter o comentário apagado (intencional, R-06); `resumo` recalculado com `findMany` da análise; last-write-wins em PATCH concorrente.

**Crítico — RF-014 / TSD-009 (01/09/2026):** **aprovado**, sem desvios de escopo. Todos os critérios com evidência (91 unit + 32 e2e): `totalPaginasPdf` contado do PDF e persistido no `criar` (`null` sem falhar quando não parseável), exposto no `GET /analises/:id`; `paginaReferencia` no PATCH validada contra `totalPaginasPdf` (`1..N` \| `≥1` quando `null` \| `null` para limpar → senão `422`) **sem** disparar a R-06; `lerPagina` mantido como seam documentado. Menores não bloqueantes: `contarPaginasPdf` roda no caminho do request de `criar` (aceitável no MVP; mover para o worker se pesar); `pdf-lib` loga warnings de parse em PDFs malformados (ruído, tratado pelo `catch`); 3º parâmetro `totalPaginasPdf` de `validarEResolverPatch` com default `null` (mantém call sites internos; o chamador real passa explícito); `pdf-lib` traz 3 avisos `npm audit` **transitivos do `prisma`** (não da nova lib).

**Documentador (31/08/2026):** TSD-001, RF-006, RF-001/004/018, RF-002, RF-005/RF-007 e RF-009 **fechados e mergeados** (`main` `96ff8fb`). Ciclo de RF-008/011/017/TSD-008 **fechado, aguardando merge** (branch `backend/rf-008-revisao-requisito`). SDD §7 ("Revisar requisito" — contrato). Roadmap RF-008/011/017 → `implementada` (branch). `questoes-abertas.md`: **P-03 fechada** (R-06). Detalhe em `.ai-dev/audit.md`.

**Documentador (01/09/2026):** ciclo RF-014/TSD-009 **fechado e mergeado** (`main` `19f308b`, push feito; branch removida). SDD §7 (Criar análise → `total_paginas_pdf`; Revisar requisito → `paginaReferencia`; Abrir análise → `totalPaginasPdf`; `GET /analises/:id/pdf` sem param de página), §8 (`analise`, `avaliacao_requisito`, `ArmazenamentoPdfPort`), §9 (decisão RF-014). Roadmap RF-014 → `implementada` (branch) + notas do ciclo. `questoes-abertas.md`: **P-05 parcialmente fechada**. Detalhe em `.ai-dev/audit.md` (`01/09/2026`).

**Crítico — RF-012/013/015 / TSD-010 (01/09/2026):** **aprovado**, sem desvios de escopo. Todos os critérios com evidência (97 unit + 36 e2e): trava por obrigatório não verificado → `422` com `requisitosPendentes` (não obrigatório não bloqueia; obrigatório `NAO_SE_APLICA` não verificado bloqueia); `200` na conclusão com `CONCLUIDA` + `concluidaEm` + responsável; `200` idempotente na reconclusão (mesmo `concluidaEm`); `409` fora de `PRONTA_PARA_REVISAO`; `404`; transição por `updateMany` condicional. `analistaId`+`analistaNome` no `GET /analises/:id`. Menores não bloqueantes: `concluir` relê a análise inteira no fim (via `abrir`); `analistaNome` de análises antigas mostra sempre o analista corrente (analista único — P-10); a transição condicional cobre a corrida de status, não a de conteúdo.

**Documentador (01/09/2026):** ciclo RF-012/013/015/TSD-010 **fechado e mergeado** (`main` `56f83bd`). SDD §7 ("Concluir análise" — contrato final), §8 (`analise`: `analista_id`/`concluida_em` expostos, transição condicional). `questoes-abertas.md` P-01/P-02 anotadas (conclusão interna, auditoria mínima). Roadmap RF-012/013/015 → `implementada`. Detalhe em `.ai-dev/audit.md` (`01/09/2026`).

**Crítico — RF-016 / TSD-011 (01/09/2026):** **aprovado**, sem desvios de escopo. Todos os critérios com evidência (106 unit + 39 e2e): `GET /analises/:id/relatorio` numa análise `CONCLUIDA` → `200 application/pdf`, `Content-Disposition: inline; filename="relatorio-analise-<id>.pdf"`, corpo `%PDF-`; modelo com NUP/objeto/responsável/datas/resumo e, por área, requisitos por `ordem` com `codigo`/`titulo`/norma formatada/página condicional/`statusFinal`; `409` se `!= CONCLUIDA`; `404`; nada persistido. Menores não bloqueantes: o texto do PDF não é assertável por extração (`pdfkit` não devolve texto) — a lógica mora em `montarModeloRelatorio` (puro, testado), o render tem smoke via `pdf-lib`; fuso fixado em `America/Sao_Paulo`; documento montado em memória (ok no volume do MVP).

**Documentador (01/09/2026):** ciclo RF-016/TSD-011 **fechado, aguardando merge** (branch `backend/rf-016-relatorio`). SDD §7 ("Relatório PDF" — contrato final). `questoes-abertas.md` **A-04 fechada para o MVP** (sob demanda, sem persistir). Roadmap RF-016 → `implementada` (branch) + notas + marco "backend de features do MVP completo". Detalhe em `.ai-dev/audit.md` (`01/09/2026`).

## 4. Decisões relevantes

| Decisão                                                                                                                                                                                                                      | Onde está documentada                                                |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| Histórico de perguntas ao responsável + respostas                                                                                                                                                                            | `docs/perguntas-e-respostas.md` (anexar cada nova rodada)            |
| Nome do projeto: LicIA Analisadora                                                                                                                                                                                           | `START_HERE.md` e método (`{{NOME_DO_PROJETO}}` substituído)         |
| PRD aprovado como baseline do MVP; 12 questões movidas para índice próprio                                                                                                                                                   | `docs/product/prd.md`, `docs/product/questoes-abertas.md`            |
| Relatório PDF final (RF-016) e persistência do PDF de entrada (RF-018) entram no MVP                                                                                                                                         | `questoes-abertas.md` R-01, R-02                                     |
| MVP sem autenticação e sem identidade: analista único e fixo; RF-003 e "sem permissão" pós-MVP                                                                                                                               | `questoes-abertas.md` R-03, P-10; SDD §2, §9                         |
| Stack: Node.js + TypeScript + NestJS + PostgreSQL + Prisma; 1 contêiner + Postgres gerenciado                                                                                                                                | SDD §2; `docs/engineering/decisions/001-framework-backend-nestjs.md` |
| Processamento assíncrono em processo, estado no banco (sem Redis no MVP)                                                                                                                                                     | SDD §4, §9                                                           |
| IA e (futura) identidade como portas com adapter stub + real                                                                                                                                                                 | SDD §3, §6, §8                                                       |
| Relatório PDF gerado sob demanda, não persistido                                                                                                                                                                             | SDD §9; `questoes-abertas.md` A-04                                   |
| Monorepo com duas frentes: `backend/` (Pablo) e `frontend/` (Vinicius), em branches paralelas, mergeadas depois                                                                                                              | `docs/product/roadmap.md`; SDD §2; esta sessão                       |
| Fundação fatiada em TSD-001 (backend) e TSD-002 (frontend), independentes                                                                                                                                                    | `docs/engineering/specs/001-*`, `002-*`                              |
| Frontend: Next.js + Vitest + RTL; camada de dados atrás de seam, fixtures até haver contrato por RF                                                                                                                          | TSD-002                                                              |
| RF-006: base de requisitos por importador de arquivo externo (CSV), não lista em código                                                                                                                                      | `roadmap.md` RF-006; TSD-003                                         |
| RF-006: `area` é campo livre com allowlist (não enum); `norma_referencia` estruturada; sem coluna de versão (imutabilidade + `ativo=false`); `titulo`/`descricao`/`norma*`/`obrigatorio` imutáveis, `ordem`/`ativo` mutáveis | TSD-003; `questoes-abertas.md` A-03                                  |
| Testes de integração/e2e rodam com PostgreSQL embutido (`embedded-postgres`), sem Docker; `E2E_EXTERNAL_DB` para banco externo na CI                                                                                         | `backend/test/embedded-postgres.globalSetup.ts`; `quality-gates.md`  |

## 5. Pendências

- [x] `test:e2e` verde — PostgreSQL embutido, sem Docker.
- [x] TSD-001 + RF-006/TSD-003 mergeadas na `main` (`13b111e`).
- [x] TSD-004 (RF-001/004/018) mergeada na `main` (`9dd6792`).
- [x] P-06 (contrato da listagem) fechado — R-05.
- [x] RF-002/TSD-005 mergeada na `main` (`57b170f`).
- [x] RF-005/RF-007/TSD-006 mergeada na `main` (`67ff89f`).
- [x] RF-009/TSD-007 mergeada na `main` (`96ff8fb`).
- [x] P-03 (quando exigir comentário) fechado — R-06.
- [x] RF-008/011/017/TSD-008 mergeada na `main` (`ad2e8cd`).
- [x] RF-014/TSD-009 mergeada na `main` (`19f308b`, push feito).
- [x] RF-012/013/015/TSD-010 (backend) mergeada na `main` (`56f83bd`).
- [x] RF-016/TSD-011 (backend) — relatório PDF final; 106 unit, 39 e2e, Crítico ✅; mergeada na `main` (com a linha do frontend). **Fecha as features de backend do MVP.**
- [x] Frontend TSD-002 + RF-002/TSD-011 + RF-001+RF-004/TSD-012 mergeados na `main` (01/09/2026).
- [x] RF-010 / TSD-013 (frontend) — tela de análise mergeada na `main` (`28c9e8c`).
- [x] RF-007 / TSD-014 (frontend) — status sugerido pela IA; Crítico ✅; mergeada na `main` (`cbe9f5b`, branch empurrada).
- [x] RF-009 / TSD-015 (frontend) — filtros por status + visão inicial priorizando não conformes; Crítico ✅; mergeada na `main` (`2e8f6d5`, branch empurrada).
- [x] RF-008 / TSD-016 (frontend) — modal "Alterar parecer"; Crítico ✅; `npm run ci` verde (164 testes); mergeada na `main` (branch `frontend/rf-008-parecer` empurrada).
- [x] RF-011 / TSD-017 (frontend) — checkbox de "verificado" interativo; Crítico ✅; mergeada na `main` (`5fc5730`, branch empurrada).
- [x] RF-017 / TSD-018 (frontend) — justificativa visível + erro de revisão fiel; Crítico ✅; mergeada na `main` (`bbbf7b8`, branch empurrada).
- [x] RF-014 / TSD-019 (frontend) — visor de PDF + referência de página clicável + editor; Crítico ✅; `npm run ci` verde (214 testes); mergeada na `main` (`d9fc9b4` + `dbfca12`).
- [x] RF-012 / TSD-020 (frontend) — botão "Concluir análise" + modal de confirmação na branch `frontend/rf-012-conclusao` (02/09/2026); Crítico ✅ (sem bloqueadores); `npm run ci` verde (240 testes); smoke conjunto contra o NestJS real feito (`frontend/docs/visual-reference/rf-012/`).
- [x] RF-016 / TSD-021 (frontend) — botão "Baixar relatório" na análise concluída na branch `frontend/rf-016-relatorio` (02/09/2026); Crítico ✅ (sem bloqueadores); `npm run ci` verde (250 testes); smoke conjunto contra o NestJS real feito (`frontend/docs/visual-reference/rf-016/`). **Último RF do MVP frontend — marco "MVP frontend completo" registrado (§1 + roadmap).**
- [x] **Frontend:** RF-012 (`aa9bb3f`) e RF-016 (`b6e7f0f`) mergeados na `main` por fast-forward, branches empurradas. **MVP frontend completo.**
- [x] **backend — CORS habilitado** (`35ee46d`, branch `backend/cors`): `app.enableCors` + `CORS_ORIGINS`; `npm run ci` do backend verde. `test:e2e` não roda nesta máquina (Postgres embutido recusa sob usuário admin) — Pablo pode querer re-rodar.
- [x] **Smoke conjunto RF-014 contra o backend real** (02/09/2026): a máquina tem um serviço **PostgreSQL 17** (só o `@embedded-postgres` recusa sob admin). Criada a base `licia`, migrations + seed, `npm run start` do NestJS em `:3000`, frontend em `:3201` com `NEXT_PUBLIC_API_BASE_URL`. Verificado: `POST /analises` → processa → `totalPaginasPdf=4`; `GET /analises/:id/pdf` `inline` renderiza no `<iframe>`; `PATCH { paginaReferencia: 3 }` grava no banco sem disparar a R-06; preflight `OPTIONS` `204` com o CORS de `35ee46d`. Evidência em `frontend/docs/visual-reference/rf-014/rf014-real-*.png`.
- [ ] Follow-up (frontend, RF-014): num deploy com CSP, `frame-src`/`X-Frame-Options` do backend precisa permitir a origem do frontend (em dev não há CSP); fallback "abrir o PDF" para browser sem visor de PDF nativo.
- [ ] Nota de ambiente: o `@embedded-postgres` do `backend/test:e2e` não sobe sob usuário admin, mas há um **PostgreSQL 17 do sistema** rodando — `backend/test:e2e` pode usar `E2E_EXTERNAL_DB` + `DATABASE_URL` apontando para ele (não testado ainda; o dev usa esse Postgres para o smoke manual).
- [ ] Follow-up (frontend, RF-011): com fixtures a marcação de "verificado" não persiste entre reloads (andaime); N `PATCH` concorrentes em itens distintos podem deixar o `resumo` de estado refletindo uma resposta fora de ordem até o próximo toggle. Some com backend real.
- [ ] **Integração frontend ↔ backend:** contratos conferidos campo a campo contra o `main` do backend (pós-RF-016) e batendo; **CORS agora habilitado** no backend → dá para o smoke com os dois processos (ver `frontend/README.md` > "Rodar contra o backend real"). Colisão de numeração: `docs/engineering/specs/011-relatorio-pdf.tsd.md` (backend RF-016) vs `011-listagem-analises-frontend.tsd.md` — alinhar com a dupla.
- [ ] **Integração da IA real (A-02 / TSD-022 + TSD-023)** — `AnaliseIaOpenAiAdapter` na branch `backend/ia-openai` (TSD-023 = commit `c781067`, empilhado sobre a TSD-022; **mergeiam juntas**). TSD-022: Crítico ✅ (condicional), `npm run ci` 126 unit / `test:contract` 2/2 / `test:e2e` 39/39. TSD-023 (adapter vira despachante texto-primário / Files-API-fallback; dep nova `pdfjs-dist 3.11.174`): Crítico ✅ (condicional), `npm run ci` **exit 0** (jest unit **138 passed / 21 suites / 0 skipped**) / `test:contract` 2/2 / `test:e2e` 39/39. **Falta:** aprovação de merge do usuário + **smoke ponta-a-ponta com a `OPENAI_API_KEY` real** — edital de 142 páginas / 13,8 MB (o que estourou o contexto no smoke da TSD-022) + 1 PDF escaneado (fallback); registrar em checkpoint/audit. Desvios em TSD-022 §13 e TSD-023 §13.
- [ ] **Dívida de segurança rastreada — CVE-2024-4367 (`pdfjs-dist@3.11.174`, high).** Introduzida pela TSD-023; mitigada (`isEvalSupported: false` + `disableFontFace`/`useSystemFonts: false` + o adapter só extrai texto, nunca rasteriza) e **aceita pelo usuário como dívida rastreada em 02/09/2026**. Follow-up obrigatório: migrar para `pdfjs-dist` v4+ / `unpdf` quando a config de testes do backend for para ESM (hoje `ts-jest` CJS aborta o `import()` dinâmico dessas libs); ao fechar, re-rodar `npm audit` e confirmar que a CVE saiu. Registrada em `questoes-abertas.md` A-09.
- [ ] **Decisão de versionamento pendente com o usuário:** `backend/scripts/dev-db.mjs`, `backend/scripts/seed-demo.mjs`, `.claude/launch.json` (untracked, não versionados nos ciclos A-02 / TSD-022 / TSD-023 — `backend/scripts/` e `.claude/launch.json` seguem no `git status` como untracked).
- [ ] **A-08** (`questoes-abertas.md`): valor definitivo do limiar de fallback (`MIN_MEDIA_CHARS_POR_PAGINA`, hoje fixo em 100) + tratamento de editais que passem de 128k tokens **mesmo como texto** — decidir se o smoke real encontrar.
- [ ] Follow-up (frontend, RF-008): com fixtures a alteração de parecer não persiste entre reloads (andaime — igual a "criar análise"); a validação da fixture exige comentário sempre (mais estrita que a R-06 do backend). Some quando houver backend real.
- [ ] Follow-up (frontend, RF-012): `focus()` no `useEffect` de mount roda com a ref `null` no `AlterarParecerModal` (o `ConcluirAnalise` já foi corrigido com `useEffect([montado])`) — alinhar os dois. Em modo fixtures o caminho "verificar o último → concluir → sucesso" não fecha (`marcarVerificado` não persiste, `concluirAnalise` relê e acha o obrigatório pendente) — só a fixture `"7"` conclui limpo; some com backend real (coberto pelo smoke).
- [ ] Follow-up (frontend): o caminho "parecer diverge da IA" (RF-007) só tem caso real com RF-008; até lá vive de fixture forjada + screenshot.
- [ ] Follow-up (frontend, RF-009): re-sincronizar os chips de filtro em navegação back/forward do browser (hoje `?requisitos=` só é lido na montagem); confirmar contraste AA do chip "Não se aplica" ativo; substituir a fixture id "7" (sem não conformes) por caso real quando houver base/backend conjunto.
- [x] Follow-up (frontend): `next lint` → `eslint .` (feito no ciclo de RF-002).
- [ ] Follow-up: migrar `package.json#prisma` para `prisma.config.ts` antes do Prisma 7.
- [ ] Follow-up (frontend): smoke de `/`, do modal "Nova análise" e da tela de análise contra o backend real quando houver ambiente conjunto (os gateways HTTP só passaram por teste de contrato).
- [ ] Questões abertas: P-07 (lista real de requisitos), P-05 (**parcialmente fechada** — a *sugestão* de página já existe no adapter OpenAI, mas só se confirma no smoke real; falta a definição de "quando a ausência é válida"), A-05/P-09 (armazenamento do PDF), P-11 (máscara do NUP), A-07 (re-seleção de `PROCESSANDO` preso). **A-02 resolvida** (ciclo A-02 / TSD-022 → `questoes-abertas.md` R-07). Ver `docs/product/questoes-abertas.md`.
- [ ] Confirmar se `docs/licia-analisadora-product-discovery.md` está versionado neste repo (citado no PRD).
- [ ] Limpar branches já mergeadas (`backend/tsd-001-fundacao-tecnica`, `backend/rf-001-criar-analise`, `backend/rf-002-listagem`, `backend/rf-005-processamento`, `backend/rf-009-abrir-analise`, `backend/rf-008-revisao-requisito`) — local + remota. (`backend/rf-014-pdf-pagina` já removida.)

## 6. Riscos / pontos de atenção

- Contrato da capacidade de análise por IA fechado no ciclo A-02 / TSD-022 + TSD-023 (`AnaliseIaOpenAiAdapter`, `questoes-abertas.md` R-07). O teste de contrato da `AnaliseIaPort` roda hoje só contra o stub; o adapter OpenAI tem unit com o SDK e o `pdfjs-dist` mockados. **A divergência stub × OpenAI real só se resolve no smoke ponta-a-ponta com a `OPENAI_API_KEY`** — enquanto ele não roda, existe risco residual de a OpenAI recusar a forma do `responses.create` (plano B: `chat.completions` + `response_format`, TSD-022 §9) e o caminho de **texto por página** da TSD-023 (marcadores `=== Página N ===`, `input_text`, sem `files.create`) nunca foi exercitado contra a API real — o smoke da TSD-023 usa o edital de 142 páginas (caminho texto) + 1 PDF escaneado (fallback Files API).
- Protótipo (`Prototipo Licia Analisadora/`) diverge do PRD em pontos registrados (`context-map.md`, seção "Contexto visual"): 4 vs. 3 status, semântica da área Técnica, "responsável" por linha. PRD/SDD vencem.
- Ausência de identidade no MVP não pode virar dívida difícil: manter `AnalistaAtualProvider` isolado e `analista_id` em todas as queries desde a TSD-001.
- Frontend real é uma aplicação Next.js separada; este repo é o backend.

## 7. Próximo passo recomendado

1. **Backend (Pablo):** o ciclo da **integração da IA real (A-02 / TSD-022 + TSD-023)** foi percorrido — `AnaliseIaOpenAiAdapter` na branch `backend/ia-openai`, Crítico ✅ (condicional) nas duas TSDs, sem bloqueadores de Dev; gates da TSD-023 verdes (`ci` exit 0 / **138 unit** / 21 suites · `contract` 2/2 · `e2e` 39/39). O adapter agora manda ao modelo o **texto do PDF por página** (`=== Página N ===`) e cai para a Files API só em PDF escaneado. **Próximo passo: o usuário aprova o merge de `backend/ia-openai` na `main` (TSD-022 + TSD-023 juntas) e fornece a `OPENAI_API_KEY` para o smoke ponta-a-ponta** (`IA_ADAPTER=openai` + `PROCESSAMENTO_AUTO=true`): (a) o **edital de 142 páginas / 13,8 MB** do smoke de 02/09 deve chegar a `PRONTA_PARA_REVISAO` sem `input exceeds the context window`, pelo caminho **texto** (sem `warn` de fallback), com sugestões que não são todas `NAO_SE_APLICA` e algumas com `paginaReferencia`; (b) um **PDF puramente escaneado** deve emitir o `warn` de fallback e ainda produzir resultado. Registrar ambos em checkpoint/audit. Se a OpenAI recusar a forma do `responses.create`, plano B da TSD-022 §9. Decisões paralelas: versionar ou não `backend/scripts/*` + `.claude/launch.json`; e o follow-up da **CVE-2024-4367** (migrar `pdfjs-dist` v4+/`unpdf` após o `ts-jest` ir para ESM + re-`npm audit`). Com o merge + smoke, **o MVP backend fica completo**.
2. **Frontend (Vinicius):** RF-002/RF-001/RF-004/RF-010/RF-007/RF-009/RF-008/RF-011/RF-017/RF-014 mergeados na `main` (+ `backend/cors`). RF-012/TSD-020 e **RF-016/TSD-021 (o último RF do MVP frontend)** implementados e aprovados pelo Crítico nas branches `frontend/rf-012-conclusao` e `frontend/rf-016-relatorio`; falta o **push das duas + merge na `main`** (RF-016 sai de RF-012). **Com isso o MVP frontend fica completo — não há próximo RF de frontend.** O que resta é acabamento: os follow-ups do §5 (A11y unificada dos botões desabilitados; re-sync de `?requisitos=` no back/forward; contraste AA do chip "Não se aplica"; brasão oficial; etc.) e o smoke de `/` + "Nova análise" contra o backend real — nenhum é bloqueio de release do MVP.
3. Cada ciclo mergeia no `main` quando o Documentador fecha. As duas frentes commitam na `main` em paralelo — merges (não só fast-forward) são esperados quando as linhas divergem; o conflito real fica só nos 3 docs compartilhados (`checkpoint`, `roadmap`, `audit`).

### Ajuste de processo (a partir de RF-014)

Nos ciclos RF-002 a RF-008/011/017 os dois pontos de aprovação humana (PM→Engenheiro e Engenheiro→Dev) foram **absorvidos nas perguntas do PM** — a User Story e a TSD foram escritas e implementadas sem uma parada explícita para o responsável validar cada artefato. As User Stories estão em `docs/product/roadmap.md` e as TSDs em `docs/engineering/specs/` (003–008), disponíveis para revisão retroativa; tudo passou por testes + Crítico e está mergeado. A partir de RF-014, o PM apresenta a User Story e **para**; o Engenheiro apresenta a TSD e **para**; só então o Dev implementa.

## 8. Prompt de retomada

```text
Leia START_HERE.md, .ai-dev/context-map.md e este checkpoint.
Identifique a spec ativa; se não houver, identifique se há uma spec candidata aguardando ativação.
Resuma objetivo, escopo, critérios de aceite e validações obrigatórias da spec ativa.
Identifique o menor próximo passo seguro.
Identifique riscos ou questões abertas antes de implementar.
Não altere arquivos.
Não implemente nada ainda.
Não atualize este checkpoint ainda.
Espere instrução explícita de construção/implementação.
```

<!-- Ao evoluir o projeto, mantenha este checkpoint enxuto e o histórico detalhado em .ai-dev/audit.md, em vez de acumular tudo aqui. -->
