import { Module } from '@nestjs/common';

/**
 * Módulo de Relatório (SDD §6) — geração sob demanda do relatório PDF da
 * análise concluída. Vazio na fundação: entra no ciclo de RF-016.
 */
@Module({})
export class RelatorioModule {}
