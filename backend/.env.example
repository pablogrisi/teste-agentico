# Porta HTTP do serviço
PORT=3000
NODE_ENV=development

# Conexão com o PostgreSQL (o docker-compose sobe um em localhost:5432)
DATABASE_URL="postgresql://licia:licia@localhost:5432/licia?schema=public"

# Analista único e fixo do MVP (não há autenticação nesta versão — ver SDD §2/§9)
ANALISTA_ATUAL_ID="analista-mvp"
ANALISTA_ATUAL_NOME="Analista MVP"

# Origens permitidas em CORS (lista separada por vírgula). Vazia = reflete a origem
# da requisição (ok no MVP sem autenticação). Ex. com o frontend em dev:
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001
CORS_ORIGINS=

# Adapter da capacidade de análise por IA: "stub" (padrão, determinístico) ou "openai" (GPT real — TSD-022)
IA_ADAPTER=stub
# Só usados quando IA_ADAPTER=openai:
#   OPENAI_API_KEY é obrigatória; IA_MODELO default gpt-4o; IA_BASE_URL opcional (proxy);
#   IA_MAX_REQUISITOS_POR_CHAMADA é o corte de requisitos por chamada ao modelo (o adapter divide em lotes).
OPENAI_API_KEY=
IA_MODELO=gpt-4o
IA_BASE_URL=
IA_MAX_REQUISITOS_POR_CHAMADA=200

# Diretório onde o adapter de filesystem grava os PDFs de entrada
ARMAZENAMENTO_PDF_DIR=./var/pdfs

# Tamanho máximo (MB) aceito para o PDF de entrada na criação da análise
ANALISE_PDF_TAMANHO_MAX_MB=25

# Processamento da análise (RF-005)
IA_TIMEOUT_MS=120000
PROCESSAMENTO_INTERVALO_MS=5000
# true: worker roda no boot + varredura periódica + disparo ao criar. false: só manual (usado nos testes).
PROCESSAMENTO_AUTO=true
